#include <stdio.h>
#include<math.h>
int main() {
    
    // double x;
    // scanf("%lf", &x);

    // int ans = round(x);
    // printf("%d", ans);

    // int x;
    // scanf("%d", &x);
    // int ans = sqrt(x);
    // printf("%d", ans);

    // int a, b;
    // scanf("%d %d", &a, &b);
    // int ans = pow(a, b);
    // printf("%d", ans);

    int a;
    scanf("%d", &a);
    int ans = abs(a);
    printf("%d", ans);

    return 0;
}